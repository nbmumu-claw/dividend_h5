# 分红记录提醒 — 实现总结

> 供小程序参考实现的架构文档。覆盖数据来源、事件提取、预览窗口、用户交互、防重复和缓存策略。

---

## 1. 整体架构

```
用户自选持仓 → 拉取分红日历事件 → 窗口过滤 → 去重 → 展示提醒卡片 → 用户确认/忽略
```

核心文件：

| 文件 | 职责 |
|------|------|
| `src/utils/dividendCalendar.ts` | 从数据源拉取分红事件（已确认 + 预估） |
| `src/utils/dividendReminder.ts` | React hook：过滤窗口内未处理事件，生成待确认列表 |
| `src/components/DividendReminderCard.tsx` | UI 组件：展示提醒卡片，提供「确认录入」「忽略」操作 |
| `src/utils/cache.ts` | 通用 localStorage TTL 缓存 |
| `src/utils/holdings.ts` | 持仓计算（交易流水、摊薄成本），分红录入会插入一条 dividend 交易 |
| `src/utils/tax.ts` | 税后计算 |
| `api/dividend-history.js` | 代理：东方财富分红数据（A股） |
| `api/yahoo-chart.js` | 代理：Yahoo Finance 行情/分红（港股） |

---

## 2. 数据来源

### 2.1 A股 — 东方财富 API

**接口：** `datacenter-web.eastmoney.com/api/data/v1/get`

**参数：**
```
reportName=RPT_SHAREBONUS_DET
columns=ALL
filter=(SECURITY_CODE="002142")
pageNumber=1
pageSize=50
sortColumns=REPORT_DATE
sortTypes=-1
```

**关键字段：**
- `ASSIGN_PROGRESS` — 进度：`实施分配` / `股东大会决议通过` / `董事会决议通过` / `特别分配`
- `IMPL_PLAN_PROFILE` — 方案说明，如 `"10派40.00元"`，解析出每股股息
- `PRETAX_BONUS_RMB` — 税前每10股派息，兜底解析
- `EQUITY_RECORD_DATE` — 股权登记日
- `EX_DIVIDEND_DATE` — 除息日
- `REPORT_DATE` — 报告期（如 2024-12-31 = 2024年报）

### 2.2 港股 — Yahoo Finance

**接口：** `query1.finance.yahoo.com/v8/finance/chart/{ticker}`

**Ticker 转换：** 内部4-5位代码 → `0001.HK`（补零到4位 + `.HK`）

**关键字段：** `chart.result[0].events.dividends`，每笔含 `date`(unix秒) 和 `amount`(每股金额)

**财年归属逻辑：** 港股末期息通常在次年上半年派发
- 1-7月除息 → 归上一财年
- 8-12月除息 → 归当年

### 2.3 手动补录

`MANUAL_DIVIDEND_EVENTS`（`dividendCalendar.ts:15`）和 `MANUAL_DIVIDEND_HISTORY`（`dividendHistory.ts:7`）用于接口未能覆盖的已知分红事件（如云南白药中期分红）。查询时自动合并，按 `recordDate` 去重。

---

## 3. 事件类型与状态

```typescript
interface DividendEvent {
  code: string
  name: string
  recordDate: string   // YYYY-MM-DD
  exDate?: string      // YYYY-MM-DD 除息日
  perShare: number     // 税前每股
  status: 'confirmed' | 'estimated'
}
```

### 3.1 已确认事件（confirmed）

**A股：** `ASSIGN_PROGRESS` 包含 `实施分配` / `特别分配` / `股东大会决议通过`，且 `EQUITY_RECORD_DATE` 非空。

**港股：** `date * 1000 <= today` 的历史分红。

### 3.2 预估事件（estimated）

用于未来未公布登记日的分红预告。

**A股：** 当 `ASSIGN_PROGRESS` = `董事会决议通过`（已公告金额但未定登记日），取同一报告期（年报 Q4/中报 Q2）最近3笔已实施记录的登记日做平均月日推算：
```
1. 找到近3次同期已实施分配的实际登记日
2. 取月日平均值
3. 年报分红归属下一年（如 2024年报 → 2025年登记）
4. 若平均日期已过当年 → 顺延一年
5. 跳过周末（周六 → 周一，周日 → 周一）
```

**港股：** 取最近2年历史股利记录，按月分组取平均日期和金额，预测当年或明年同期：
```
1. 按月份分组最近2年所有历史股利
2. 若当年该月已有 confirmed 事件 → 跳过
3. 平均日期 > 今天 → 预测当年，否则 → 预测明年
4. 跳过周末
```

---

## 4. 提醒窗口与过滤

`usePendingDividends` hook 的核心过滤逻辑（`dividendReminder.ts:74-103`）：

```
窗口：登记日前 1 天（WINDOW_AHEAD=1）~ 登记日后 5 天（WINDOW_BACK=5）

过滤条件（全部满足才进入提醒）：
1. event.status === 'confirmed'        // 只提醒已确认事件，estimated 不提醒
2. 登记日日期在窗口内                   // 太远的不提醒，太久前的也不提醒
3. 用户未 dismiss                       // localStorage 存储已忽略列表
4. 该股未录入相近分红                   // 已录入的 ±7 天分红视为已处理
5. 该股持仓 > 0                         // 无持仓不提醒
6. 登记日当时持仓 > 0                   // 登记日当天无股不提醒（用 sharesAsOf 查）
```

输出 `PendingDividend` 按登记日升序排列。

---

## 5. 用户交互

### 5.1 确认录入（confirm）

```
1. 生成一笔 type='dividend' 的 Transaction：
   - qty = sharesAsOf(txs, recordDateTs)  // 登记日当时持仓股数
   - price = afterTax(perShare, stock)    // 税后每股
   - ts = recordDate timestamp
   - gross = perShare                     // 税前毛额（备查）
2. append 到该股的 transactions 数组
3. 摊薄成本自动更新（分红额冲减成本基数）
```

分摊薄成本公式（`holdings.ts`）：
```
净持仓 = Σ买入 - Σ卖出
摊薄成本价 = (Σ买入额 - Σ卖出额 - Σ分红额) / 净持仓
```

其中 `Σ分红额` = 每笔分红 × 该笔登记日当时持仓（与录入顺序无关）。

### 5.2 忽略（dismiss）

将 `"{code}@{recordDate}"` 存入 localStorage key `dividend-dismissed`（Set → JSON 序列化）。同一笔事件不再提醒。

---

## 6. UI 展示

`DividendReminderCard` 组件支持两种模式：

**list 模式**（Watchlist 页，多只股票汇总）：
- 红色边框卡片，铃铛图标
- 标题：`待确认分红 N 笔`
- 每行显示：股票名、登记日(MM-DD)、每股股息、股数、税后到手金额
- 按钮：`确认录入`（红色） + `忽略`（灰色）

**single 模式**（HoldingDetail 页，单只股票）：
- 同上但不显示股票名

---

## 7. 去重与防重复策略

| 层级 | 机制 | 位置 |
|------|------|------|
| 接口缓存 | localStorage TTL 缓存，到次日凌晨4点过期 | `dividendCalendar.ts` — `cacheGet`/`cacheSet` |
| 用户忽略 | `dividend-dismissed` localStorage，永久 | `dividendReminder.ts:9,28-37` |
| 已录入检查 | 已有 ±7 天内登记的 dividend 交易则跳过 | `dividendReminder.ts:40-43` — `alreadyRecorded` |
| 推送后立即隐藏 | dismiss 后 `useState` 更新，触发重新渲染过滤掉该项 | `dividendReminder.ts:114-121` |
| 接口返回空 | catch 返回 `[]`，不崩溃 | `dividendCalendar.ts:255-257` |

---

## 8. 缓存策略

| 缓存项 | TTL | 存储位置 |
|--------|-----|----------|
| 分红日历事件 | 到次日凌晨 4:00 | `localStorage`（key: `dh_cache_calEvent2:{code}`） |
| 分红历史 | 含近2年数据 → 1天；仅历史数据 → 30天 | `localStorage`（key: `dh_cache_dividendHistory:{code}`） |
| 股票行情 | 5分钟 | 内存 |
| 汇率 | 6小时 | 内存 |

---

## 9. 集成点

### 9.1 Watchlist 页（自选列表）
```tsx
const pendingDiv = usePendingDividends(watchlist)
// ...
<DividendReminderCard
  items={pendingDiv.items}
  onConfirm={(it) => { pendingDiv.confirm(it); showToast(`已录入 ${it.name} 分红`) }}
  onDismiss={pendingDiv.dismiss}
  variant="list"
/>
```

### 9.2 HoldingDetail 页（单只股票持仓详情）
```tsx
const pendingDiv = usePendingDividends(useMemo(() => stock ? [stock] : [], [stock]))
// ...
<DividendReminderCard
  items={pendingDiv.items}
  onConfirm={(it) => { pendingDiv.confirm(it); showToast('已录入分红') }}
  onDismiss={pendingDiv.dismiss}
  variant="single"
/>
```

---

## 10. 小程序适配建议

### 10.1 数据层
- **A股分红数据**：沿用东方财富 `datacenter-web` API，小程序可直接调，无需代理
- **港股分红数据**：沿用 Yahoo Finance chart API，注意小程序需配置服务器域名白名单
- **缓存**：小程序用 `wx.setStorageSync` / `wx.getStorageSync` 替代 localStorage
- **手动补录**：同样保留一个本地常量数组兜底

### 10.2 核心流程（不变）
```
自选股列表 → 遍历调 API → 合并手动事件 → 过滤 confirmed + 窗口内 + 未忽略 + 未录入 → 展示提醒
```

### 10.3 数量最少逻辑
- 提醒范围：登记日前1天 ~ 登记日后5天
- 已录入判定：该股交易记录中是否有登记日 ±7 天的分红记录
- 忽略状态：`wx.setStorageSync('dividend-dismissed', JSON.stringify([...]))`
- API 缓存：用 `wx.setStorageSync` + 过期时间戳判断

### 10.4 小程序还原代码
```javascript
// 分红提醒 hook 核心逻辑（小程序版）
function usePendingDividends(stocks) {
  const [items, setItems] = useState([])
  const [dismissed, setDismissed] = useState(() => {
    try {
      const raw = wx.getStorageSync('dividend-dismissed')
      return new Set(raw ? JSON.parse(raw) : [])
    } catch { return new Set() }
  })

  // 遍历持仓股拉取日历事件
  useEffect(() => {
    const holders = stocks.filter(s => computeHolding(s).shares > 0)
    if (holders.length === 0) { setItems([]); return }
    Promise.all(holders.map(s => fetchCalendarEvents(s.code, s.isHK)))
      .then(events => {
        const today = new Date()
        const hi = today.getTime() + 1 * 86400000   // +1天
        const lo = today.getTime() - 5 * 86400000   // -5天

        const result = events.flat().filter(e =>
          e.status === 'confirmed' &&
          recordTs(e.recordDate) <= hi &&
          recordTs(e.recordDate) >= lo &&
          !dismissed.has(`${e.code}@${e.recordDate}`) &&
          !alreadyRecorded(stockTxs, e.recordDate) &&
          sharesAsOf(stockTxs, recordTs(e.recordDate)) > 0
        )
        // 计算税后到手金额
        setItems(result.map(e => ({
          ...e,
          qty: sharesAsOf(stockTxs, recordTs(e.recordDate)),
          net: afterTax(e.perShare, stock) * sharesAsOf(stockTxs, recordTs(e.recordDate))
        })))
      })
  }, [stocks])

  const confirm = (item) => {
    // 生成 dividend 交易记录，写入 stocks.transactions
  }
  const dismiss = (item) => {
    const next = new Set(dismissed)
    next.add(`${item.code}@${item.recordDate}`)
    setDismissed(next)
    wx.setStorageSync('dividend-dismissed', JSON.stringify([...next]))
  }

  return { items, confirm, dismiss }
}
```
