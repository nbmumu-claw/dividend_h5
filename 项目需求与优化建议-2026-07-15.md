# 红利助手：项目需求与优化建议

> 评估日期：2026-07-15  
> 当前决定：FIFO 红利税需求暂缓；已上线的“股权登记日按当地时间当天结束计算”修复保留。

## 一、项目现状判断

“红利助手”已经从股息率查询工具发展为面向个人投资者的轻量投资账本，当前覆盖：

- A股、港股、美股、场外基金
- 股票发现与股息率比较
- 自选和真实持仓
- 买入、卖出、分红流水
- 手续费和持仓成本
- 多账户管理
- 组合收益和资产分类
- 分红日历与提醒
- 决策矩阵、网格策略
- 本地备份和云同步
- 截图识别交易
- Vercel Analytics

产品核心定位建议收敛为：

> 帮助长期分红投资者准确记录持仓、追踪未来现金流，并判断组合是否健康。

当前不建议继续横向增加独立页面，更应该优先解决：

1. 用户能否相信数字。
2. 用户是否理解收益口径。
3. 用户每天打开后能得到什么行动信息。
4. 数据是否安全、可恢复。
5. 长期录入是否足够轻松。

## 二、当前做得比较好的部分

### 1. 已形成交易流水基础

`src/utils/holdings.ts` 已定义买入、卖出、分红三种流水，每笔记录包含类型、数量、价格、时间和分红税前金额。这为持仓快照、历史收益、现金流和未来 FIFO 等能力留下了基础。

### 2. 分红已进入实际持仓计算

分红不是简单使用今天的持股数，而是根据分红日期回看当时持仓，可以支持历史补录和分批交易。

### 3. 多账户设计已经可用

账户分别保存持仓、手续费配置和交易流水；切换账户时会用对应费率重新计算持仓。

### 4. 已具备数据迁移能力

Zustand 持久化版本已经升级到 v9，并包含多个历史迁移，开始考虑老用户数据兼容。

### 5. 已有云同步相关测试

当前测试覆盖云同步、同步分支、板块迁移和统计范围迁移，说明同步逻辑并非完全依赖人工测试。

## 三、P0：必须优先处理的问题

### 1. 禁止在 localStorage 明文保存密码

`src/components/AuthModal.tsx` 当前会把邮箱和密码直接写入 `localStorage`。同源脚本、XSS、恶意浏览器插件或公共电脑环境都可能导致密码泄露。

建议：

- 删除“记住密码”选项。
- 只允许记住邮箱。
- 登录状态交给 CloudBase Session。
- 升级时主动删除历史 `auth-remember.password`。

验收标准：

- `localStorage` 不再出现密码。
- 老用户升级后历史明文密码被清理。
- 刷新页面仍可通过正常 Session 保持登录。
- 退出登录后 Session 和敏感数据被清除。

优先级：最高  
预计工作量：0.5～1天

### 2. 完善手工备份

目前手工备份主要导出持仓、账户、自定义股票、静态编辑、隐藏股票和自定义板块，但没有完整覆盖：

- `categoryOverrides`
- `gridPrefs`
- `simStrategy`
- `statsScope`
- 汇率设置
- 部分用户偏好

建议建立唯一的序列化入口：

```ts
exportAppData()
importAppData()
```

手工备份和云同步共用同一份数据结构，避免两套字段长期漂移。备份格式升级为带 `schemaVersion`、`exportedAt` 和 `appVersion` 的 v3。

验收标准：

1. 准备包含多账户、流水、板块、分类、网格和模拟策略的数据。
2. 导出备份。
3. 清空应用数据。
4. 导入备份。
5. 所有字段和导出前完全一致。
6. 老版本备份仍能导入。

优先级：最高  
预计工作量：1～2天

### 3. 给财务核心计算补测试

当前测试没有充分覆盖持仓与分红计算。建议至少覆盖：

- 单次和分批买入
- 部分和分批卖出
- 同日多笔买卖
- 超额卖出
- 清仓后重新买入
- 买入和卖出手续费
- 单次和多次分红
- 分红前后买卖
- 负摊薄成本
- 港币、美元、人民币换算
- 历史补录后的重新计算

业务日期应该决定持仓资格，录入时刻只用于排序，不应因为晚上补录而影响股权登记资格。

验收标准：

- `holdings.ts`、`fees.ts` 和关键汇率计算都有单元测试。
- 所有边界条件都有明确断言。
- 编辑或删除历史交易后能正确重算。

优先级：最高  
预计工作量：2～3天

### 4. 阻止超额卖出

新增或编辑卖出时，应按卖出日期计算当时可用持仓，而不是只看今天的最终持仓。

提示示例：

```text
卖出数量不能超过该日期当时的可用持仓（20股）
```

验收标准：

- 新增卖出时校验历史可用持仓。
- 编辑卖出时临时排除原记录再计算。
- 错误提示包含当日可卖数量。
- 不能通过负数、空值或异常小数绕过。
- 修改历史买入导致后续卖出失效时明确提示。

优先级：最高  
预计工作量：1天

## 四、P1：最值得开发的产品需求

### 1. 本月分红首页

成熟用户更关心自己的持仓和未来现金流。建议新增“概览”首页，或者让收益页承担首页职责，显示：

- 本月预计分红
- 本月已经到账
- 待确认分红
- 下一笔股权登记和预计到账
- 本年度预计分红和实际分红
- 去年同期对比
- 未来12个月分红分布
- 最近行情更新时间

首页首先回答：

> 这个月能收到多少钱，下一笔什么时候来，有没有需要处理的事情？

验收标准：

- 用户进入应用后10秒内能看到当月现金流。
- 预计和实际分红明确分开。
- 点击金额可以追溯到具体股票和记录。
- 无数据时提供明确的下一步操作。

预计工作量：3～5天

### 2. 分红对账闭环

建议把状态拆分为：

```text
预计 → 已确认方案 → 已到股权登记日 → 待到账 → 已到账
```

建议流程：

1. 系统发现分红方案。
2. 根据登记日持仓计算预计金额。
3. 到账日前后进入“待确认”。
4. 用户点击“确认到账”。
5. 自动生成分红交易。
6. 用户可以修改实际到账金额。
7. 系统显示预计与实际差额。

未来分红记录可逐步区分：

```ts
{
  recordDate: string
  exDate?: string
  paymentDate?: string
  confirmedAt?: number
  perShareGross: number
  actualAmount?: number
  source: 'automatic' | 'manual'
}
```

验收标准：

- 自动分红不会重复录入。
- 用户能看到未确认到账项目。
- 确认后自动生成对应流水。
- 编辑和删除流水后状态能同步恢复。
- 预计金额和实际金额可分别统计。

预计工作量：4～7天

### 3. 拆分收益口径

当前摊薄成本使用“买入额－卖出额－分红额”计算，有助于判断回本进度，但不等同于券商成本价。

建议显示：

- 当前持仓成本
- 未实现盈亏
- 已实现盈亏
- 累计现金分红
- 总收益
- 回本进度

当前指标可以保留，但建议命名为“分红摊薄成本”“回本成本”或“剩余投入成本”。

验收标准：

- 每个数字都可以展开查看来源。
- 交易编辑后所有口径同步重算。
- 清仓股票仍能显示已实现收益和累计分红。
- 不同币种先在原币种计算，再统一换算展示。
- 页面提供简短口径说明。

预计工作量：5～8天

### 4. 展示数据可信度

当前多个请求失败后会静默降级。离线可用是优点，但用户不知道看到的是实时数据还是旧缓存。

建议记录并展示：

```ts
{
  value: number
  source: string
  updatedAt: number
  status: 'live' | 'cached' | 'stale' | 'failed'
}
```

页面可以显示：

```text
18只更新成功 · 2只使用缓存 · 1只获取失败
更新时间：14:32
```

验收标准：

- 接口失败不再表现为无提示的旧数字。
- 用户可以手动重试。
- 缓存数据不会被误认为实时数据。
- 接口返回0、无数据和请求失败能够区分。

预计工作量：3～5天

### 5. 券商流水导入

建议支持 CSV、Excel、券商成交记录复制粘贴和截图批量确认。

推荐流程：

```text
选择文件 → 识别字段 → 预览 → 检测重复 → 展示冲突 → 用户确认 → 写入交易
```

初步去重依据：

```text
账户 + 股票代码 + 交易日期 + 类型 + 数量 + 价格
```

相似记录不应静默丢弃，应交给用户确认。

验收标准：

- 导入前不修改真实数据。
- 显示新增、重复、冲突和无效记录数量。
- 支持撤销最近一次导入。
- 导入完成后持仓和收益自动重算。
- 重复导入同一文件不会产生重复流水。

预计工作量：5～10天

## 五、P2：增强留存的需求

### 1. 新用户引导

第一轮只引导完成三个动作：

1. 添加一只股票。
2. 录入一笔买入。
3. 查看预计年度分红。

决策矩阵、网格策略、自定义分类和模拟加仓等高级功能后置。

### 2. 分红目标

支持年度分红目标、月均分红目标、单只股票目标股数和目标股息率，并展示目标完成度。

### 3. 股息率与分红提醒

支持：

- 当前股息率达到目标
- 股价进入目标区间
- 分红方案同比下降
- 分红方案发生变化
- 临近股权登记日
- 预计已经到账但尚未确认

### 4. 组合健康检查

先展示透明指标，不急于做黑盒评分：

- 单一股票最大占比
- 前五大持仓占比
- 单一行业占比
- A/H/美股比例
- 高周期资产占比
- 未来12个月分红月份分布
- 现金流最薄弱月份
- 分红同比下降股票数量

### 5. 历史趋势

支持年度分红、月度现金流、持仓市值、累计投入、累计分红和分红覆盖投入比例等趋势。历史市值趋势应等快照数据可靠后再实现。

## 六、数据模型优化建议

### 1. 区分业务日期与系统时间

当前 `ts` 同时承担交易日期、排序时间和录入时间。未来建议逐步升级为：

```ts
interface Transaction {
  id: string
  type: 'buy' | 'sell' | 'dividend'
  tradeDate: string
  createdAt: number
  updatedAt: number
  qty: number
  price: number
}
```

分红还应区分股权登记日、除权日、到账日和用户确认时间。

### 2. 每笔交易增加稳定 ID

未来同步、批量导入、撤销和冲突合并都需要稳定交易 ID。建议使用 `crypto.randomUUID()`，并为老数据迁移生成 ID。

### 3. 保存原始币种

交易、分红和手续费应保留原始币种：

```ts
currency: 'CNY' | 'HKD' | 'USD'
```

展示时再按当前汇率换算，不应把人民币换算值作为唯一事实数据。

### 4. 区分预计与真实数据

建议区分预计每股分红、已确认每股分红和实际到账金额，预计数据不能覆盖用户已经确认的真实记录。

## 七、工程结构优化

### 1. 拆分超大页面

当前主要页面规模：

| 页面 | 大约行数 |
|---|---:|
| `YieldGrid.tsx` | 1068 |
| `Discovery.tsx` | 785 |
| `Watchlist.tsx` | 757 |
| `Portfolio.tsx` | 733 |
| `Settings.tsx` | 581 |
| `Calendar.tsx` | 561 |
| `Matrix.tsx` | 495 |

建议按页面容器、数据选择器、纯计算函数、列表组件、详情弹窗、编辑表单和页面级 Hook 拆分。优先抽纯计算函数，不要为了拆文件制造大量只有一层转发的组件。

### 2. 按领域拆 Zustand Slice

建议内部拆分为：

```text
portfolioSlice
accountSlice
discoverySlice
settingsSlice
strategySlice
```

仍然可以保留一个 Zustand Store 和一个持久化入口。

### 3. 建立统一派生选择器

建议集中实现：

```ts
selectAccountHoldings()
selectPortfolioSummary()
selectDividendSummary()
selectMonthlyCashflow()
selectStockPerformance()
```

避免同一指标在不同页面出现不同答案。

### 4. 统一错误模型

网络失败可以继续自动降级，但应留下来源、错误代码、发生时间和是否可重试，供页面展示。

### 5. 页面级错误隔离

在全局 Error Boundary 之外增加局部隔离，一个图表失败不应导致整个收益页不可用。

## 八、性能优化

### 1. 缩小主包

当前生产构建主 JS 包约 1 MB。重点排查：

- CloudBase SDK 是否进入首包
- 云同步能否在登录后动态加载
- 静态股票数据是否占据主包
- 截图识别是否进入首包
- Recharts 是否仅在相关页面加载

建议目标：

- 主包压缩前低于 500 KB。
- 首屏不加载图表、云同步管理和截图识别。
- 非登录用户不加载完整认证代码。

### 2. 统一请求与缓存

统一 TTL、数据来源、强制刷新、请求去重、失败重试和并发上限。多个页面同时请求同一股票时应复用同一个 Promise。

### 3. 避免重复聚合计算

全持仓汇率换算、分类聚合、分红汇总、排序和过滤应集中为稳定的纯计算函数和选择器。

## 九、同步与数据可靠性

### 1. 显示同步状态

建议显示：

```text
已同步
正在同步
本地有未同步修改
同步失败
发生冲突
最后成功：今天 14:32
```

### 2. 防止跨设备覆盖

每个数据文档或账户至少保存 `updatedAt`、`deviceId`、`schemaVersion` 和 `revision`。复杂度允许时，按稳定交易 ID 合并，而不是整份持仓覆盖。

### 3. 增加删除保护

删除账户、清空自选等操作建议提供临时恢复点、撤销入口和最近一次本地快照。

## 十、文档问题

当前 README 已明显落后于产品：

- 仍描述为5个页面。
- 未体现美股、基金、日历、网格、多账户和云同步。
- 仍写“A股红利免税”，容易造成错误理解。
- 生产代理说明与当前 Serverless Functions 架构不完全一致。
- 持久化名称和旧文档描述存在漂移。

建议 README 作为开发者文档，用户税务说明放到应用内独立页面，并注明税务信息仅供参考，应以券商实际代扣和现行法规为准。

## 十一、建议暂缓的需求

现阶段不建议优先开发：

- FIFO 红利税
- 复杂智能选股评分
- 社区和排行榜
- 大量技术指标
- 自动交易
- AI 投资建议
- 复杂回测
- 更多独立图表页面

这些能力都依赖可靠的基础数据和清楚的收益口径。底层没有稳定前，新增分析只会放大误差。

## 十二、建议路线图

### 第一阶段：安全与可信，约1周

- 移除明文密码
- 完整备份 v3
- 超额卖出校验
- 持仓与分红核心测试
- 修订 README 和税务说明

交付结果：数据更安全，备份真正可恢复，核心数字有测试保障。

### 第二阶段：分红闭环，约1～2周

- 本月分红概览
- 预计、待到账、已到账状态
- 一键确认到账
- 数据更新时间和来源
- 网络失败提示

交付结果：用户每天打开应用都有明确价值。

### 第三阶段：收益口径，约2周

- 当前持仓成本
- 已实现盈亏
- 未实现盈亏
- 累计分红
- 总收益
- 清仓股票历史收益

交付结果：用户能够解释每一个收益数字。

### 第四阶段：降低录入成本，约1～2周

- CSV/Excel 导入
- 批量预览
- 重复检测
- 冲突处理
- 最近一次导入撤销

交付结果：项目从偶尔使用的工具变成可长期维护的投资账本。

### 第五阶段：组合决策，约1周

- 分红目标
- 股息率提醒
- 集中度分析
- 未来12个月现金流
- 分红下降提醒

交付结果：从记录工具升级为长期投资决策辅助工具。

## 十三、最终优先级

如果现在只能选择五项：

1. 删除 localStorage 明文密码。
2. 修复不完整备份。
3. 补持仓、手续费和分红计算测试。
4. 实现本月分红与到账确认闭环。
5. 拆分已实现收益、未实现收益和累计分红。

如果只能选择一个新需求：选择“本月分红 + 待到账确认”。

如果只能选择一个技术优化：选择“财务核心计算测试”。

如果只能选择一个紧急修复：选择“移除明文密码”。
